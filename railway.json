{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm -C packages/shared build && pnpm -C apps/api exec prisma generate && pnpm -C apps/api build"
  },
  "deploy": {
    "startCommand": "pnpm -C apps/api start",
    "healthcheckPath": "/health"
  }
}
