{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm -C packages/shared build && pnpm -C apps/api exec prisma generate && pnpm -C apps/api build"
  },
  "deploy": {
    "preDeployCommand": [
      "pnpm -C apps/api exec prisma migrate deploy && pnpm -C apps/api seed:prod"
    ],
    "startCommand": "pnpm -C apps/api start:server",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
