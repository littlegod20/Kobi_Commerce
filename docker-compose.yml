services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kobi_ecommerce
    ports:
      - "5432:5432"
    volumes:
      - kobi_ecommerce_pg:/var/lib/postgresql/data

volumes:
  kobi_ecommerce_pg:
